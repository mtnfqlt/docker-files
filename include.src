#!/bin/bash -e


for arg in "$@"; do
  #eval "$(echo "$arg" | awk '{print $1=$N}')"
  echo $arg
done

# for arg in "$@"; do
#   case $arg in
#     work_dir=*)
#       work_dir=${arg#*=} ;;
#     context_dir=*)
#       export CONTEXT_DIR=${arg#*=} ;;
#     docker_file=*)
#       export DOCKER_FILE=${arg#*=} ;;
#     repo_url=*)
#       export REPO_URL=${arg#*=} ;;
#     branch=*)
#       export BRANCH=${arg#*=} ;;
#   esac
# done
