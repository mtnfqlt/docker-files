#!/bin/bash -e

arg_list=$(echo "$@" | sed 's/ /|/g' | sed 's/=/ /g')
#arg_list="$@"

for arg in $arg_list; do
  echo "$arg"
done

# for arg in $arg_list; do
#   variable=$(echo "$arg" | cut -d'|' -f1)
#   value=$(echo "$arg" | cut -d'|' -f2- | sed 's/|/ /' | sed 's/[[:space:]]*$//')
#   #eval "$variable='$value'"
# done

# echo "$arg_list"
# exit
